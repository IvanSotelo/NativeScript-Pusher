import { DemoSharedBase } from '../utils';
import {} from '@ivansotelo/nativescript-pusher';

export class DemoSharedNativescriptPusher extends DemoSharedBase {
  testIt() {
    console.log('test nativescript-pusher!');
  }
}
